upstream backend {
    server backend:8080;
    keepalive 16;
}

# Rate limiting
limit_req_zone $binary_remote_addr zone=api:10m rate=30r/m;
limit_req_zone $binary_remote_addr zone=auth:10m rate=5r/m;

server {
    listen 80;
    server_name api.jobfresh.in;

    # Let's Encrypt challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Redirect HTTP → HTTPS once cert is ready
    # Uncomment after running certbot:
    # return 301 https://$host$request_uri;

    client_max_body_size 10M;

    gzip on;
    gzip_vary on;
    gzip_types application/json;

    # API — proxy to Spring Boot
    location /api/ {
        limit_req zone=api burst=20 nodelay;

        proxy_pass http://backend;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_connect_timeout 5s;
        proxy_read_timeout 30s;
    }

    # Strict rate limit on auth endpoint
    location /api/v1/auth/token {
        limit_req zone=auth burst=3 nodelay;

        proxy_pass http://backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Block everything else
    location / {
        return 404;
    }
}

# HTTPS — uncomment after certbot
# server {
#     listen 443 ssl;
#     server_name api.jobfresh.in;
#
#     ssl_certificate /etc/letsencrypt/live/api.jobfresh.in/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/api.jobfresh.in/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#
#     location /api/ { ... same as above ... }
#     location /api/v1/auth/token { ... same as above ... }
#     location / { return 404; }
# }
